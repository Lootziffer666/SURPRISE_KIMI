# Snow Resource Game

Generated from the code blocks in the supplied chat export.

## Run

```bash
npm install
npm run dev
```

Applied corrections:

- `InventoryStack` is instantiated with `parent: this.player`.
- The erroneous `lockedExpansion.position.x = 0` line was removed.
